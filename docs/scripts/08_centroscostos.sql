CREATE TABLE `dynapolls`.`centroscostos` (
  `ccid` BIGINT(18) NOT NULL AUTO_INCREMENT,
  `ccdsc` VARCHAR(75) NOT NULL,
  `ccest` CHAR(3) NOT NULL,
  `cctipo` CHAR(3) NOT NULL,
  PRIMARY KEY (`ccid`));